# Shell scripts must keep LF endings or they break on macOS/Linux with
# "bad interpreter: /usr/bin/env bash^M", regardless of the cloner's core.autocrlf.
*.sh  text eol=lf
*.ps1 text eol=crlf
*.md  text
*.json text
